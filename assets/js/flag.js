/* Loaded in the head, not deferred: marks the page as scripted before it paints, so the phone header can
   show its menu button from the first frame. A file rather than an inline script, so any host's
   script-src 'self' allows it without a hash. */
document.documentElement.classList.add("js");
